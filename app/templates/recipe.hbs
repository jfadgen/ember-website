<Recipe
  @recipe={{this.model}}
  @title={{this.model.title}}
  @reference={{this.model.reference}}
  @cooking_summary={{this.model.cooking_summary}}
  @category={{this.model.category}}
  @ingredients={{this.model.ingredients}}
/>
