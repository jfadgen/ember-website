{{#each this.model as |recipe|}}
  <Recipe
    @title={{recipe.title}}
    @reference={{recipe.reference}}
    @cooking_summary={{recipe.cooking_summary}}
    @category={{recipe.category}}
    @ingredients={{recipe.ingredients}}
  />
{{/each}}
