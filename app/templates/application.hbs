<div class="container">
  <div class="menu">
    {{#link-to "index"}}
      <h1>
        <em>Homepage</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to "about" class="menu-about"}}
        About
      {{/link-to}}
      {{#link-to "recipes" class="menu-recipes"}}
        Recipes
      {{/link-to}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>
